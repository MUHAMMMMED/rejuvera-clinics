server {
    listen 80;
    listen [::]:80;
    server_name rejuveracenter.com www.rejuveracenter.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name rejuveracenter.com www.rejuveracenter.com;

    client_max_body_size 500M;

    ssl_certificate /etc/ssl/cert.pem;
    ssl_certificate_key /etc/ssl/key.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # FRONTEND
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # BACKEND API
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # MEDIA - ✅ FIX: direct from volume folder
    location /media/ {
        alias /root/static_media/;
        expires 30d;
        add_header Cache-Control "public";
        add_header Access-Control-Allow-Origin "*";
    }

    # STATIC
    location /static/ {
        alias /root/static_backend/;
        expires 30d;
        add_header Cache-Control "public";
    }

    error_log /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
}


# # تأكد من صحة الكونفيج
# nginx -t

# # لو ظهر "test is successful"
# systemctl reload nginx

# # تأكد إن الصور موجودة في الفولدر
# ls /root/static_media/



# # شوف الصور فين جوه الـ Docker container
# docker exec backend ls /app/media/

# # انسخهم للـ host
# docker cp backend:/app/media/. /root/static_media/

# # بعدين reload nginx
# systemctl reload nginx



# # 1. اديله permissions
# chmod -R 755 /root/static_media/
# chmod -R 644 /root/static_media/*/*

# # 2. المشكلة إن /root نفسه محمي - لازم تدي nginx إذن يدخله
# chmod 755 /root

# # 3. reload nginx
# systemctl reload nginx